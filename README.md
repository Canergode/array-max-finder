# Array Mx Finder 
This project finds the maximum value in a float array entered by the user.It validates the array size and uses a for loop to compare elements.

## What I Learned 
- How to declare and use arrays in C
- How to validate user input with a while loop
- How to find the maximum value in an array
- How to iterate through array elements
